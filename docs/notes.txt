TOBE model in \GenerativeLSTM\output_files\simulation_files 
Questions:
    - Previous: 
    - invocation in SIMOD to prosimos https://github.com/AutomatedProcessImprovement/Prosimos
    - Modify Merge Declarative to call prosimos (json:ASIS) (bpmn:TOBE)
Notes:
    - Attributes of execution (Resources, Timestamps)

Tasks:
    - Logica del Merger (Etructura de prosimos y bimp)
        - Merger
    - Diagrama de entradas y salidas
    - Files (details):
        1. BPMN Original
        2.a LSTM CSV -> SM3 and probs -> BMPN_TOBE
        2.b SIMOD (SM3, bimp o prosimos) -> BMPN_ASIS JSON
        3. MERGER (BMPN_TOBE,JSON_ASIS) -> JSON_MERGED, BPMN_TOBE_BIMP
        4. SIMULATOR (prosimus(BMPN_TOBE,JSON_MERGED) - bimp(BPMN_TOBE_BIMP) -> Stats, CSV)
    - IA Driven Process Optimization
    - Create 2 Diagrams:
        - Modified Implementation: generates tobe with simod (Details: Input and Outputs)
        - Original Implementation (Details: Input and Outputs)
    - Pipeline Automated Implementation.
        - Folder (optional)
        - Adjust read of folders generated by simod
        - Compress CSV
        - Adjust logic of merger between ASIS and TOBE of the JSON.
    - Original Pipeline.
        - Genarate the BPMN with BIMP format WHAT-IF (without simod).
Tasks 24 Jun:
    - Ajutar Carpeta para reusar el modelo + parametro para entrenar
    - Nueva rama del generative ajustado
    - Ajustar BIMP (Buscar en SIMOD)

Preguntas Daniel:
    - <Preguntas de logica y de entrada y salida>
    - Cuales eran las consideraciones a la hora de generar el modelo de simulación objetivo combinando el estocastico tobe con los parametros asis? Tips de formato o logica especial
Respuestas Daniel:
    - Rama Generative
Tasks 11 July
    - Diagramas Con detalles como 
    - BIMP Generar el archivo (tomar la logica)